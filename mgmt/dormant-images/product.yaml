---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service-kub-deployment
  labels:
    app: product-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: "joakimengqvist/mgmt-product-service:latest"
          env:
            - name: DSN
              value: "host=host.minikube.internal port=5432 user=postgres password=password dbname=products sslmode=disable timezone=UTC connect_timeout=5"
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "512Mi"
              cpu: "0.5"
            requests:
              memory: "256Mi"
              cpu: "0.25" 
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: product-service-kub-service
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - protocol: TCP
      name: web-port
      port: 80
      targetPort: 80