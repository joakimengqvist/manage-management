---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notes-service-kub-deployment
  labels:
    app: notes-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notes-service
  template:
    metadata:
      labels:
        app: notes-service
    spec:
      containers:
        - name: notes-service
          image: "joakimengqvist/mgmt-notes-service:latest"
          env:
            - name: DSN
              value: "host=host.minikube.internal port=5432 user=postgres password=password dbname=notes sslmode=disable timezone=UTC connect_timeout=5"
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "512Mi"
              cpu: "0.5"
            requests:
              memory: "256Mi"
              cpu: "0.25" 
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: notes-service-kub-service
spec:
  type: ClusterIP
  selector:
    app: notes-service
  ports:
    - protocol: TCP
      name: internal-port
      port: 80
      targetPort: 80