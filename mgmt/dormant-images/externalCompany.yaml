---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-company-service-kub-deployment
  labels:
    app: external-company-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: external-company-service
  template:
    metadata:
      labels:
        app: external-company-service
    spec:
      containers:
        - name: external-company-service
          image: "joakimengqvist/mgmt-external-company-service:latest"
          env:
            - name: DSN
              value: "host=host.minikube.internal port=5432 user=postgres password=password dbname=companies sslmode=disable timezone=UTC connect_timeout=5"
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "512Mi"
              cpu: "0.5"
            requests:
              memory: "256Mi"
              cpu: "0.25" 
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: external-company-service-kub-service
spec:
  type: ClusterIP
  selector:
    app: external-company-service
  ports:
    - protocol: TCP
      name: internal-port
      port: 80
      targetPort: 80