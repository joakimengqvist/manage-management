---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-service-kub-deployment
  labels:
    app: invoice-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: invoice-service
  template:
    metadata:
      labels:
        app: invoice-service
    spec:
      containers:
        - name: invoice-service
          image: "joakimengqvist/mgmt-invoice-service:latest"
          env:
            - name: DSN
              value: "host=host.minikube.internal port=5432 user=postgres password=password dbname=invoice sslmode=disable timezone=UTC connect_timeout=5"
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "512Mi"
              cpu: "0.5"
            requests:
              memory: "256Mi"
              cpu: "0.25" 
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: invoice-service-kub-service
spec:
  type: ClusterIP
  selector:
    app: invoice-service
  ports:
    - protocol: TCP
      name: internal-port
      port: 80
      targetPort: 80